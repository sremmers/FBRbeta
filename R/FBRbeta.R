#' FBRbeta: A package for using the FBR method on fMRI data
#'
#' The package provides several functions: a method for shuffling the timings,
#' see \code{\link{timingmatrix}}, a function to obtain the beta's from X and
#' y \code{\link{betafunc}}, a code to make boxcars of a particular timing
#' \code{\link{boxcars}}, a code that combines \code{\link{timingmatrix}} and
#' \code{\link{boxcars}} in a function called \code{\link{designmatrix}}, and a summary
#' code that combines the previous codes \code{\link{fbrbetafunc}}.
#'
#' @section FBRbeta functions:
#' The functions.
#'
#' @docType package
#' @name FBRbeta

